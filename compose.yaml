services:
  sumire:
    build:
      context: .
      dockerfile: Dockerfile
    image: sumire:local
    env_file:
      - .env
    init: true
    restart: unless-stopped
    volumes:
      - ./instructions:/app/instructions:ro
      - ./skills:/app/skills:ro
      - events:/app/.events
      - state:/app/.telegramagent
      - whisper-cache:/app/.cache/whisper

volumes:
  events:
  state:
  whisper-cache:
