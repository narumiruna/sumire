---
"@narumitw/sumire": patch
"@narumitw/sumire-url-content": patch
---

Enable Pi's native read, bash, edit, and write tools in every bot session, remove the coding-tool opt-in flag, and require a non-empty Telegram whitelist before starting sessions. Make the URL-content CLI skill available by default.
